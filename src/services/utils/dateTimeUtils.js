export const getTimeDiff = (from, to) => {
    if (to > from) {
        let today = new Date();
        let date = today.getFullYear()+'-'+(today.getMonth()+1)+'-'+today.getDate();
        let timeFrom = from + ':00';
        let timeTo = from + ':00'
    }
};